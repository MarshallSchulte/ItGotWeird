<% provide(:title, @user.username) %>
<div class="row">
  <div class="col-md-4">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.username %>
      </h1>
  </div>
  <div class="col-md-4">
	<%= link_to 'New Blog', new_blog_path, :class => 'btn btn-default btn-lg'  %>
  </div>
</div>
